clc
clear all
close all
%
% profile clear
% profile on

Cparams100 = load('Cparams100.mat');
Cparams100 = Cparams100.CParams;

% im = imread('../TestImages/one_chris.png');
% Cparams.thresh = 1.9;
% tic
% dets = ScanImageFixedSize(Cparams, im);
% toc
% % tic
% % dets = ScanImageOverScale1(Cparams100, im, 0.02, 0.4, 0.006);
% % toc
% DisplayDetections(im, dets);
% fdets = PruneDetections(dets);
% DisplayDetections(im, fdets);



im = imread('../TestImages/facepic2.jpg');
% Cparams.thresh = 3;
tic
dets = ScanImageOverScale(Cparams100, im, 0.2, 1.3, 0.06);
toc
% tic
% dets = ScanImageOverScale1(Cparams100, im, 0.02, 0.4, 0.006);
% toc
DisplayDetections(im, dets);
fdets = PruneDetections(dets);
DisplayDetections(im, fdets);
