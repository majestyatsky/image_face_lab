function dets = ScanImageOverScale(Cparams, im, min_s, max_s, step_s)
scs = min_s:step_s:max_s;
dets = cell(length(scs),1);

for i=1:length(scs)
    a = imresize(im,scs(i));
    dets{a} = ScanImageFixedSize(Cparams, a);
    
end

dets = vertcat(a{:}); % flatten into array

end